--4.Modify the "GM Hummer" record to read "a huge interior"...
update inventory set inv_description = 'Do you have 6 kids and like to go offroading? The Hummer gives you a huge interior with an engine to get you out of any muddy or rocky situation.'
where 
inv_make = 'GM' and inv_model = 'Hummer'

--6.Update all records in the inventory table to add "/vehicles" to the
update inventory set inv_image = concat('/images/vehicles/',substring(inv_image,length(inv_image) - position('/' in reverse(inv_image)) + 2))
